# 开发环境Dockerfile - 支持热更新
FROM node:18-alpine

WORKDIR /app

# 复制package文件并安装依赖（利用Docker缓存）
COPY package*.json ./
RUN npm ci --silent

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 开发模式启动（支持热更新）
CMD ["npm", "start"] 